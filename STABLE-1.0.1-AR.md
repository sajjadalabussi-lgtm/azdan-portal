# أزدان بورتال 1.0.1 Stable

## ما تم فحصه

- التأكد من وجود صفحة تقرير المشروع: `/admin/client/[id]/report`.
- مطابقة روابط لوحة الإدارة مع مسارات App Router الموجودة.
- فحص الاستيرادات المحلية `@/` وعدم وجود ملفات محلية مفقودة.
- الإبقاء على حماية لوحة الإدارة وواجهات API الإدارية.
- إضافة صفحة 404 عربية بدل الصفحة الافتراضية.
- إضافة `.env.example` بدون أسرار.
- تحديث فحص الحالة إلى الإصدار `1.0.1`.

## فحص ما قبل النشر

```bash
npm install
npm run build
npm run start
```

ثم اختبر:

- `/api/health`
- `/admin`
- `/admin/clients`
- `/admin/reports`
- تقرير عميل من الزر داخل صفحة العميل
- إنشاء وتعديل عميل وتحديث ودفعة
- النسخ الاحتياطي

## ملاحظة Next.js 16

قد يظهر تحذير بأن `middleware.ts` سيُستبدل مستقبلًا باتفاقية `proxy`. التحذير لا يمنع التشغيل في النسخة الحالية، ولم يتم تغيير ملف الحماية لتجنب أي تغيير غير ضروري قبل النشر.
